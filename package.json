{
  "name": "chuchu-tamagotchi",
  "version": "1.0.0",
  "description": "ChuChu — A tiny virtual puppy pet you can feed, play with, and watch evolve!",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "no": "^0.0.1",
    "playwright": "^1.59.1"
  }
}
